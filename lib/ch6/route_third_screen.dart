import 'package:flutter/material.dart';

class RouteThirdScreen extends StatelessWidget {
  const RouteThirdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("RouteThirdScreen"),
      ),
    );
  }
}
